{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "release",
      "type": "shell",
      "command": "bun run release",
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "PATH": "${env:HOME}/.bun/bin:${env:PATH}"
        }
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
